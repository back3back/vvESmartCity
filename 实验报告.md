# 智慧城市 Android 应用开发实验报告

## 一、实验目的

1. 学习并掌握 SharedPreferences 的使用，实现数据的持久化存储
2. 学习并掌握 SQLite 数据库的使用，实现购物信息的增删改查
3. 学习并掌握 SD 卡读写操作，实现图片的存储与管理
4. 实现智慧城市应用各功能模块的数据交互功能

## 二、实验环境

- 开发工具：Android Studio
- 开发语言：Kotlin
- UI 框架：Jetpack Compose
- 架构组件：ViewModel、StateFlow、Room（SQLite）
- 目标平台：Android 12+

## 三、实验内容与过程

### （一）项目前期基础

前期已完成以下工作：
1. 搭建 Android 系统开发环境
2. 完成智慧城市主要模块界面设计，包括：
   - 环境状态值范围设计界面
   - 购物信息存储界面
   - 火焰监控界面
   - 摄像头监控界面
   - 用户注册界面
   - 查询购物信息界面
   - 智慧城市主界面
   - 农业大棚环境监控界面
3. 实现页面跳转导航逻辑

### （二）本次实验完成内容

#### 1. SharedPreferences 的使用 — 智能农业环境数据设置

**实现内容：**
- 使用 SharedPreferences 存储农业环境参数（温度范围、湿度范围、光照强度、CO 临界值）
- 实现数据的保存、读取、清空功能
- 创建 FarmSettingsScreen 界面，提供参数设置入口
- 在主界面添加"智慧农业设置"入口按钮

**关键实现：**
- 通过 SharedPreferences 将配置数据持久化到设备本地
- 管理员关闭应用再次进入时，可读取之前保存的配置
- 支持清空/重置操作，删除已保存的数据

#### 2. SQLite 数据库的使用 — 购物信息管理

**实现内容：**
- 创建 Room 数据库（ShoppingDatabase），包含订单表（ShoppingEntity）
- 实现购物信息的录入（Insert）、修改（Update）、查询（Query）、删除（Delete）
- 创建 OrderManagementScreen 界面，展示订单管理功能
- 在主界面添加"商超数据库"入口按钮

**关键实现：**
- 使用 Room 持久化库简化 SQLite 操作
- 通过 DAO 接口定义数据库操作方法
- 使用 StateFlow 实现数据库数据的实时监听与 UI 自动更新

#### 3. SD 卡读写 — 摄像头截图存储

**实现内容：**
- 创建 FileService 类，实现 SD 卡文件读写服务
- 实现 createImageDirectory() 方法，在 SD 卡创建 SmartCityImages 文件夹
- 实现 saveBitmapToStorage() 方法，将 Bitmap 保存为 JPEG 文件
- 添加 WRITE_EXTERNAL_STORAGE 权限处理（Android 12 及以下）
- 在 WarningScreen 中添加截图按钮，实现截图保存功能
- 创建 CameraCaptureScreen 界面，提供截图生成与保存功能
- 在主界面添加"摄像头截图"入口按钮

**关键实现：**
- 使用 Context.getExternalFilesDir() 获取外部存储路径
- 使用 Bitmap.compress() 将图片压缩为 JPEG 格式
- 保存成功后通过 Toast 提示用户文件保存路径

#### 4. 主界面功能模块跳转与数据交互

**实现内容：**
- 在主界面添加三个新模块入口：智慧农业设置、商超数据库、摄像头截图
- 完善 Navigation.kt 导航逻辑，注册新路由
- 实现各功能模块内的增删改查数据交互

**各模块功能：**

| 模块 | 功能 |
|------|------|
| 环境气象 | 显示温度、湿度信息，查询过往温湿度，提供穿衣/洗车/旅行建议 |
| 智能商超 | 商品列表展示、添加商品、搜索商品、拍码购物、购物车管理、结算扣减库存 |
| 预警信息 | 当前预警信息展示、过往预警信息列表、截图保存功能 |
| 智慧农业 | 农场设备与传感器信息展示、设备开关控制、环境参数设置 |

#### 5. 其他功能修复与优化

- 修复购物车数量实时更新问题（LazyColumn key 绑定 quantity）
- 修复购买后库存不减少问题（结算后刷新商品列表）
- 修复主页底部按钮被遮挡问题（启用滚动、增加底部 padding）
- 修复头像存储问题（cacheDir → filesDir，固定文件名，自动清理旧头像）

## 四、实验结果

1. SharedPreferences 存储：农业环境参数设置后保存，关闭应用重新打开可正确读取
2. SQLite 数据库：购物信息可正常录入、修改、查询、删除，数据持久化存储
3. SD 卡读写：摄像头截图可成功保存到 SD 卡，保存路径正确提示
4. 主界面跳转：各功能模块入口正常，页面跳转流畅，数据交互正常

## 五、实验总结

本次实验完成了智慧城市 Android 应用的核心数据持久化功能。通过 SharedPreferences 实现了轻量级配置数据的存储，通过 SQLite（Room）实现了结构化业务数据的增删改查，通过 SD 卡文件操作实现了图片资源的存储管理。同时完善了主界面各功能模块的跳转与数据交互，修复了多个 UI 交互问题，提升了应用的稳定性和用户体验。
